#include <iostream>

using namespace std;

int main() {
    cout << "Giá trị số nguyên của ký tự 0 la " << (int) '0' << endl;
    cout << "Giá trị số nguyên của ký tự 9 la " << (int) '9' << endl;
    cout << "Giá trị số nguyên của ký tự a la " << (int) 'a' << endl;
    cout << "Giá trị số nguyên của ký tự z la " << (int) 'z' << endl;
    cout << "Giá trị số nguyên của ký tự A la " << (int) 'A' << endl;
    cout << "Giá trị số nguyên của ký tự Z la " << (int) 'Z' << endl;
    cout << "Giá trị số nguyên của ký tự space la " << (int) ' ' << endl;
    return 0;
}