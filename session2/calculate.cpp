#include <iostream>

using namespace std;

int main() {
    cout << "Nhap lan luot 2 so a va b cach nhau boi khoang trang : ";
    int a,b;cin >> a >> b;
    cout << "a + b = " << (a+b) << endl;
    cout << "a - b = " << (a-b) << endl;
    cout << "a * b = " << (a*b) << endl;
    cout << "a / b = " << (a/b) << endl;
    cout << "a % b = " << (a%b) << endl;
    return 0;
}